Prerequisites:
- the environment variable CATALINA_HOME must be set to the install dir. of your tomcat distribution

Compile + Deploy:
- run the build file as "Ant Build" or execute "ant local_install" in the project's dir. 

Test:
http://localhost:8080/LaPizzeria/home